Test change t

React App create command: npx create-react-app reacttutorial-classcomponent

reacttutorial-classcomponent is my directory


functional compnent create shortcart key: type rfc the press enter.

same file e functional component no net to import.only type the component name.
like :
function Welcome(){
	return <h2>Welcome</h2>;
} 

then use in expoert return

export default function app(){
	return (
	<div>
           <Welcome />
	</div>
	)
}

functional start with capital letter
another way is :
function Welcome(){
	return React.createElement("<h1>",{},"Welcome");
} 
 
first parameter is which html tag we use the {} means props and finally message .


first install ES7 React/Redux/GraphQL/React-Native snippets.
the use some shortcart like: to create functional component use short cart just type rfc then enter
if you create class component then use or type rcc then press enter
Chrome web store  extension then serach react developer tools and  install and use react on browser

react Icon use just search reach icon and go to https://react-icons.github.io/react-icons/ and install it

end Test